1946787:
  requires:
    apt:
      python3-barbican:
        # wallaby:
        - min: 2:12.0.0
          max: 2:12.0.0-0ubuntu1
        # victoria:
        - min: 1:11.0.0
          max: 1:11.0.0-0ubuntu0
        # ussuri:
        - min: 1:10.0.0
          max: 1:10.1.0-0ubuntu1
  raises:
    type: hotsos.core.issues.LaunchpadBug
    message: >-
      Installed package '{package}' with version {version} has a known bug that
      prevents cinder from creating encrypted volumes. To fix this you need to
      the latest version.
    format-dict:
      package: '@requires.package'
      package: '@requires.version'
