# Both mon and osd nodes will have a ceph-mon package and service installed
# but it will typically be disabled if not in-use so we use that as a
# means of knowing what type of node it is.
requires:
  or:
    - systemd:
        ceph-osd: enabled
    - systemd:
        snap.microceph.osd: enabled
