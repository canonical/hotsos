checks:
  has_unexpected_masked: |
    len(@hotsos.core.plugins.openstack.OpenstackChecksBase.unexpected_masked_services) > 0
conclusions:
  has-unexpected-masked:
    decision: has_unexpected_masked
    raises:
      type: OpenstackWarning
      message: >-
        The following Openstack systemd services are masked: {masked}. Please
        ensure that this is intended otherwise these services may be
        unavailable.
      format-dict:
        masked: hotsos.core.plugins.openstack.OpenstackChecksBase.unexpected_masked_services:comma_join
