checks:
  has_1883089:
    input:
      path: 'var/log/neutron/neutron-l3-agent.log'
    expr: '([\d-]+) [\d:]+\.\d{3} .+AttributeError: ''NoneType'' object has no attribute ''get'''
    hint: 'AttributeError'
conclusions:
  lp1883089:
    decision: has_1883089
    raises:
      type: LaunchpadBug
      bug-id: 1883089
      message: >-
        A known neutron l3-agent bug (LP1883089) is affecting DVR floating IPs
        on this host. Please upgrade neutron to the latest version to resolve
        this issue.
